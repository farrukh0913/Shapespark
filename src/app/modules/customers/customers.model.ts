export interface ITableColumns {
    id: string;
    value: string
};

export interface ICustomersList {
    id: string;
    name: string;
    description: string;
    color: string;
    noOfGroups: number;
    logo: string;
}
